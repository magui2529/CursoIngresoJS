function mostrar()
{

	var contador=0;
	var num;
	var negativo;
	var positivo;
	var acumuladorpos;
	var acumuladorneg;

	do{
		num=parseInt(prompt("Ingrese un numero"));
		while(isNaN(num)){
			num=parseInt(prompt("No es un numero. Ingrese un numero."));
		}
			if(contador==0){
				maximo=numero;
				minimo=numero;
				contador++;

		} if(numero<1){
			negativo+=numero;
		}
	} 

	
	
		
	
	



}//FIN DE LA FUNCIÓN